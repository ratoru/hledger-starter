[project]
name = "dashboard-exporter"
version = "0.1.0"
description = "Exporter for hledger metrics to Victoria Metrics"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.3",
]
